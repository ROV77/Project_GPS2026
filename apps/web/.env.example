# URL base de la API. En desarrollo se deja vacío: el front pega a '/api' y
# Vite la redirige a http://localhost:3000 (ver proxy en vite.config.ts).
# En producción, apunta al host real del backend, p. ej. https://api.caseritapp.cl
VITE_API_URL=
