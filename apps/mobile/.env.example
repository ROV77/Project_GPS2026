# URL base de la API que consume el mobile.
# IMPORTANTE: en un teléfono físico, "localhost" es el propio teléfono, NO tu PC.
# Usa la IP LAN de tu computador (averíguala con `ipconfig` en Windows → IPv4).
# Ej: EXPO_PUBLIC_API_URL=http://192.168.1.42:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
